///C Program to Add Two Integers (Input by the User)

#include<stdio.h>
int main()
{
	int a, b, sum;
	printf("Enter any two integers: ");
	scanf("%d %d", &a, &b);
	printf("The sum of two integers %d + %d = %d", a, b, sum=a+b);
	return 0;
}
