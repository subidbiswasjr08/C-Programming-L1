///C Program to Add Two Integers (Input by the User)

#include<stdio.h>
int main()
{
	float a, b, pro;
	printf("Enter any two numbers: ");
	scanf("%f %f", &a, &b);
	printf("The product of two numbers %.1f + %.1f = %.2f", a, b, sum=a*b);
	return 0;
}
